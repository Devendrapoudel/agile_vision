import 'package:flutter/material.dart';

class AppColors {
  // Primary — deep professional blue
  static const Color primary = Color(0xFF1B4FD8);
  static const Color primaryLight = Color(0xFFEEF2FF);
  static const Color primaryDark = Color(0xFF1338A8);

  // Backgrounds
  static const Color background = Color(0xFFF8F9FC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF1F5F9);

  // Status colors (for KPI indicators)
  static const Color success = Color(0xFF16A34A);
  static const Color successLight = Color(0xFFDCFCE7);
  static const Color warning = Color(0xFFD97706);
  static const Color warningLight = Color(0xFFFEF3C7);
  static const Color danger = Color(0xFFDC2626);
  static const Color dangerLight = Color(0xFFFEE2E2);
  static const Color info = Color(0xFF0EA5E9);
  static const Color infoLight = Color(0xFFE0F2FE);

  // Text
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textMuted = Color(0xFF94A3B8);

  // Border
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderLight = Color(0xFFF1F5F9);

  // Member theme colors
  static const Color devendra = Color(0xFF7C3AED);  // Purple — Schedule
  static const Color roshan = Color(0xFF0891B2);    // Teal — Cost
  static const Color shambhu = Color(0xFF1B4FD8);   // Blue — UI/HCI
  static const Color shiva = Color(0xFF059669);     // Green — Infrastructure
}
