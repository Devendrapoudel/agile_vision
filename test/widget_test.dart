// Placeholder widget test for AgileVision.
// The default counter scaffold test has been removed since this app does not
// use a counter widget. Add real integration tests here as the app matures.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('AgileVision smoke test placeholder', (WidgetTester tester) async {
    // TODO: add meaningful widget tests once Firebase is injectable in tests.
    expect(true, isTrue);
  });
}
